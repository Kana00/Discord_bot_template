import LogMessageDAO from '../../dao/LogMessageDAO';
import Mongo from '../../utils/Mongo';

describe('Test class LogMessageDAO', () => {

  it('LogMessageDAO-saveToDB', () => {
    expect(1+2).toBe(3);
  });

});
